import React from 'react';
import Draggable from 'react-draggable';

function Drag(props) {

  return (
    <Draggable>
      <div>
        hi
      </div>
    </Draggable>
  );
}

export default Drag;
